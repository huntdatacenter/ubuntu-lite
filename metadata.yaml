name: huntdatacenter-ubuntu-lite
display-name: |
  Ubuntu Lite
description: |
  Run an Ubuntu system, but don't do a whole lot else.
summary: |
  The smallest charm I could come up with that still looks nice.
maintainers:
  - HUNT Cloud <cloud@hunt.ntnu.no>

provides:
  ubuntu:
    interface: ubuntu

extra-bindings:
  another:

subordinate: false
